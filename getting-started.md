# Getting Started

This page is plain Markdown with no front matter.

That makes it a good example for small teams that want to start writing docs quickly without deciding on metadata conventions first.

## What this page demonstrates

- The docs layout still renders normal headings and body content.
- The page can be ordered through `.nav.yml` instead of front matter fields.
- Relative links and generated navigation continue to work for simple content pages.
