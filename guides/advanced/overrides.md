# Navigation Overrides

This page sits in a nested folder so the sample site shows deeper navigation levels.

## Why it is here

- It proves sidebar expansion and breadcrumb behavior for nested content.
- It pairs well with `.nav.yml` examples when you want to reorder pages without renaming files.
